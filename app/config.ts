const config = {
  siteName: process.env.SITE_NAME || "Developer",
  exampleEmail: process.env.EXAMPLE_EMAIL || "",
  examplePassword: process.env.EXAMPLE_PASSWORD || "",
  demoDomain: process.env.DEMO_DOMAIN || "developer.ducphu.com",
};

export default config;
